using UnityEngine;
using System.Collections;

public class LootTable : MonoBehaviour {

	// Items and their drop rate percentage:
	/*	1. Small Health Potion (30) { Add 25% max HP }
	 * 	2. Big Health Potion (15) { Add 50% max HP }
	 * 	2. Breastplate (5) { +10 to max HP } [ Remove from loot table after looted ]
	 * 	3. Helmet (5) { +5 to max HP } [ Remove from loot table after looted ]
	 * 	4. Magic Sword (5) { +5 to Damage } [ Remove from loot table after looted ]
	 */

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
